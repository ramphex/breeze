| endpoint_or_flow | token_state_used | who_sees_plaintext | what_is_returned | scope_checks | rate_limit | replay_possible | notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Token creation (`POST /backup/bmr/tokens`) | `active` | MFA-authenticated operator at creation time only | plaintext recovery token once | org-scoped snapshot lookup | authenticated app auth + MFA | no | plaintext is not stored, only hash |
| Public authenticate (`POST /backup/bmr/recover/authenticate`) | `active`, `authenticated`, legacy `used` without `completedAt` | recovery operator with plaintext token | bootstrap payload + authenticated session | token format, token hash, token org/device/snapshot linkage, expiry | per-IP + per-token | controlled | repeated auth allowed only while `authenticated` |
| Public download (`GET /backup/bmr/recover/download`) | `authenticated` | recovery environment with plaintext token | proxied or signed download target for requested path | token format, token status, token TTL, `snapshots/{token.snapshotId}/` path anchoring | per-token | bounded | status is re-checked on every request |
| Public complete (`POST /backup/bmr/recover/complete`) | `authenticated`, `used` idempotent replay | recovery environment with plaintext token | restore job id/status | token format, authenticated state, token expiry/revocation | per-IP | idempotent only | transitions token to `used` |
| Media / boot-media artifact lifecycle | admin token linkage | authenticated operator | signed artifact metadata/downloads | org + token id scope | authenticated app auth | no | token revocation/expiry expires linked artifacts |
